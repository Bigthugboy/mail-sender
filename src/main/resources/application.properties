spring.application.name=Mail-Senders

# Sender defaults

app.mail.from-address=${FROM_EMAIL}
app.mail.brevo-api-key=${BREVO_API_KEY}
app.mail.brevo-base-url=${BREVO_BASE_URL}
app.mail.reply-to=${MAIL_REPLY_TO}
app.mail.unsubscribe-mailto=${MAIL_UNSUBSCRIBE}
app.mail.max-recipients-per-request=1000
app.mail.max-send-rate-per-second=${MAIL_SEND_RATE:10}
